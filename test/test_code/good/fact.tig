/* define a recursive function */
let
  /* calculate n! */
  function fact (n : int) : int =
    if  n = 0
      then 1
      else n * fact (n - 1)
in
  if fact (6) = 720 
  then (print("Bien");print("\n"))
  else (print("Mal");print("\n"))
end
